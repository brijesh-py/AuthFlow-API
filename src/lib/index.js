const { oauth2GoogleResponse, googleApi } = require("./google.lib");
const { githubResponse, githubApi } = require("./github.lib");

module.exports = {
  oauth2GoogleResponse,
  googleApi,
  githubResponse,
  githubApi
};
